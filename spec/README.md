# Shared Specification

This directory contains non-secret, synthetic cross-platform contracts. A fixture may not contain a real Codex home, credential, auth file, session JSONL, conversation, backup, or database.

Current Phase 1 TOML ownership preserves unknown fields and tables. The documented compatibility-managed exception is limited to the existing cross-platform Provider fields in `docs/architecture/codex-toml-ownership.md`; all other fields remain preserve-only until accessible official OpenAI documentation establishes their semantics.
