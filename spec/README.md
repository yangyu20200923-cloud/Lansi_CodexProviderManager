# Shared Specification

This directory contains non-secret, synthetic cross-platform contracts. A fixture may not contain a real Codex home, credential, auth file, session JSONL, conversation, backup, or database.

Current Phase 1 TOML ownership is preserve-only until accessible official OpenAI documentation establishes managed field semantics.
